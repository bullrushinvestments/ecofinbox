interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string;
}

interface HeaderRule {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  [path: string]: { regexPattern: string; edgeTtlSeconds: number };
}

interface FrameworkSettings {
  vercel?: Record<string, any>;
  netlify?: Record<string, any>;
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  headers: Array<HeaderRule>;
  cacheStrategies: CacheStrategy;
  frameworkSettings: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: "next build",
    output: ".next"
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
  },
  headers: [
    {
      source: "/api/:path*",
      headers: [
        { key: "Access-Control-Allow-Credentials", value: "true" },
        { key: "Access-Control-Allow-Origin", value: "*" },
        { key: "Vary", value: "Origin" }
      ]
    },
    {
      source: "/(.*)",
      headers: [
        { key: "strict-transport-security", value: "max-age=31536000; includeSubDomains; preload" },
        { key: "x-content-type-options", value: "nosniff" },
        { key: "x-frame-options", value: "DENY" },
        { key: "x-xss-protection", value: "1; mode=block" }
      ]
    }
  ],
  cacheStrategies: {
    "/api/:path*": { regexPattern: "^/api/", edgeTtlSeconds: 0 },
    "/_next/static/:path*": { regexPattern: "^/_next/static/", edgeTtlSeconds: 31536000 },
    "/*": { regexPattern: "^/(.*)", edgeTtlSeconds: 0 }
  },
  frameworkSettings: {
    vercel: {
      buildCommand: "yarn run build",
      startCommand: "yarn run start"
    },
    netlify: {
      buildContext: "production",
      functions: ".vercel/functions",
      publishDir: ".next"
    }
  }
};

interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string;
}

interface HeaderRule {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  [path: string]: { regexPattern: string; edgeTtlSeconds: number };
}

interface FrameworkSettings {
  vercel?: Record<string, any>;
  netlify?: Record<string, any>;
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  headers: Array<HeaderRule>;
  cacheStrategies: CacheStrategy;
  frameworkSettings: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: "next build",
    output: ".next"
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
  },
  headers: [
    {
      source: "/api/:path*",
      headers: [
        { key: "Access-Control-Allow-Credentials", value: "true" },
        { key: "Access-Control-Allow-Origin", value: "*" },
        { key: "Vary", value: "Origin" }
      ]
    },
    {
      source: "/(.*)",
      headers: [
        { key: "strict-transport-security", value: "max-age=31536000; includeSubDomains; preload" },
        { key: "x-content-type-options", value: "nosniff" },
        { key: "x-frame-options", value: "DENY" },
        { key: "x-xss-protection", value: "1; mode=block" }
      ]
    }
  ],
  cacheStrategies: {
    "/api/:path*": { regexPattern: "^/api/", edgeTtlSeconds: 0 },
    "/_next/static/:path*": { regexPattern: "^/_next/static/", edgeTtlSeconds: 31536000 },
    "/*": { regexPattern: "^/(.*)", edgeTtlSeconds: 0 }
  },
  frameworkSettings: {
    vercel: {
      buildCommand: "yarn run build",
      startCommand: "yarn run start"
    },
    netlify: {
      buildContext: "production",
      functions: ".vercel/functions",
      publishDir: ".next"
    }
  }
};